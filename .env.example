# Used only when the persistent browser shows the corresponding login form.
# Keep these exact names and store values only in the ignored private .env.
Quick_rcm_email=
Quick_rcm_password=
prognosis_username=
prognosis_password=

# Required verbatim only after a supervised test-patient canary is approved.
# Leave blank for probe mode.
CLINICAL_WRITE_ACK=
